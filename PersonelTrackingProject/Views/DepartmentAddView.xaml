<Window x:Class="PersonelTrackingProject.Views.DepartmentAddView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PersonelTrackingProject.Views"
        mc:Ignorable="d"
        Title="DepartmentAddView" Height="200" Width="350" ResizeMode="NoResize" WindowStartupLocation="CenterScreen" Icon="/img1.png">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Label Content="Name" Margin="40,20,10,30" VerticalAlignment="Center" HorizontalAlignment="Right" />
        <TextBox x:Name="txtDepartmentName" Grid.Column="1" Background="LightGreen" Margin="10,25,40,35" />
        <Button x:Name="btnSave" Content="Save" Grid.Row="1" Margin="50,30,10,30" Click="btnSave_Click" />
        <Button x:Name="btnCancel" Content="Cancel" Grid.Row="1" Grid.Column="1" Margin="10,30,40,30" Click="btnCancel_Click" />
    </Grid>
</Window>
